/**
 * Configuración de entorno — Producción
 *
 * PARA CONECTAR AL BACKEND REAL:
 * 1. Cambiar `useMocks` a `false`
 * 2. Cambiar `apiUrl` a la URL real de tu API (ej: 'https://api.crumbs.app')
 */
export const environment = {
  production: true,
  useMocks: false,
  apiUrl: 'https://api.crumbs.app',
};
